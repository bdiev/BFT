# Быстрый старт - Админ-панель

## 🚀 Как сделать себя администратором

### Вариант 1: Через базу данных (рекомендуется)

1. Остановите сервер (если запущен)

2. Откройте базу данных SQLite:
   ```bash
   sqlite3 database.db
   ```

3. Посмотрите список пользователей:
   ```sql
   SELECT id, username, is_admin FROM users;
   ```

4. Сделайте нужного пользователя админом (замените `1` на нужный ID):
   ```sql
   UPDATE users SET is_admin = 1 WHERE id = 1;
   ```

5. Проверьте изменения:
   ```sql
   SELECT id, username, is_admin FROM users;
   ```

6. Выйдите из SQLite:
   ```sql
   .quit
   ```

7. Запустите сервер:
   ```bash
   npm start
   ```

### Вариант 2: Через PowerShell (Windows)

```powershell
# Сделать первого пользователя админом
sqlite3.exe database.db "UPDATE users SET is_admin = 1 WHERE id = 1;"

# Проверить результат
sqlite3.exe database.db "SELECT id, username, is_admin FROM users;"
```

### Вариант 3: Временный код в server.js

Добавьте этот код в `server.js` после строки с `db.serialize(() => {` (примерно строка 61):

```javascript
// ВРЕМЕННО: Делаем первого пользователя админом
db.run("UPDATE users SET is_admin = 1 WHERE id = 1", (err) => {
  if (err) console.error('❌ Ошибка:', err);
  else console.log('✅ Первый пользователь стал админом');
});
```

Запустите сервер, подождите пока выполнится код, затем **удалите** этот код.

## 📱 Как открыть админ-панель

### Способ 1: Через интерфейс
1. Войдите в приложение под учетной записью администратора
2. Нажмите на кнопку "Войти" в шапке (или на свое имя)
3. В модальном окне появится кнопка **"⚙️ Админ-панель"**
4. Нажмите на неё

### Способ 2: Прямая ссылка
Откройте в браузере:
```
http://localhost:3000/admin.html
```

## ✅ Проверка прав

После входа проверьте:
- Кнопка "⚙️ Админ-панель" видна в модальном окне аккаунта
- При открытии `/admin.html` вы видите админ-панель, а не редирект на главную

## 🎯 Основные функции

### Статистика
- Общее количество пользователей
- Количество администраторов  
- Всего записей
- Логов воды

### Управление пользователями
- 👁️ **Детали** - подробная информация о пользователе
- 🔐 **Сделать админом / Снять админа** - управление правами
- 🔑 **Сбросить пароль** - установить новый пароль для пользователя
- 🗑️ **Удалить** - удалить пользователя со всеми данными

### Поиск
Введите в поле поиска:
- Логин пользователя
- Email
- ID

## ⚠️ Важно

1. **Не удаляйте себя** - система не позволит, но будьте внимательны
2. **Не снимайте права у себя** - останетесь без доступа к админке
3. **Удаление необратимо** - все данные пользователя удаляются навсегда
4. **Сброс пароля** - пользователь НЕ получит уведомление, сообщите ему новый пароль

## 🔒 Безопасность

- Все admin endpoints защищены проверкой прав
- JWT токен проверяется на каждый запрос
- Права проверяются в базе данных (не в токене)
- Нет доступа без авторизации

## 📞 Помощь

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Убедитесь, что `is_admin = 1` в базе данных
4. Перезагрузите страницу

## 📝 Примеры SQL запросов

```sql
-- Посмотреть всех админов
SELECT id, username, email FROM users WHERE is_admin = 1;

-- Сделать пользователя админом по имени
UPDATE users SET is_admin = 1 WHERE username = 'имя_пользователя';

-- Снять права админа
UPDATE users SET is_admin = 0 WHERE id = 2;

-- Количество админов
SELECT COUNT(*) as admin_count FROM users WHERE is_admin = 1;
```

Готово! Теперь у вас есть полноценная админ-панель для управления пользователями. 🎉
