# Админ-панель - Инструкция

## Что добавлено

### 1. База данных
- Добавлено поле `is_admin` в таблицу `users` (значение 0 или 1)
- Автоматическая миграция существующих БД при первом запуске

### 2. API Endpoints

#### Проверка прав администратора
- `GET /api/admin/check` - проверить, является ли текущий пользователь админом

#### Управление пользователями (требуются права админа)
- `GET /api/admin/users` - список всех пользователей со статистикой
- `GET /api/admin/users/:id` - детальная информация о пользователе
- `POST /api/admin/users/:id/toggle-admin` - изменить права администратора
- `DELETE /api/admin/users/:id` - удалить пользователя
- `POST /api/admin/users/:id/reset-password` - сбросить пароль пользователя
  - Параметры: `{ "newPassword": "новый_пароль" }`

#### Статистика (требуются права админа)
- `GET /api/admin/stats` - общая статистика системы

### 3. Интерфейс админ-панели
- **Файл**: `admin.html`
- **Стили**: `admin-style.css`
- **Логика**: `admin.js`

#### Возможности:
1. **Статистика**
   - Общее количество пользователей
   - Количество администраторов
   - Всего записей
   - Всего логов воды

2. **Управление пользователями**
   - Просмотр списка всех пользователей
   - Поиск по логину, email, ID
   - Просмотр детальной информации о каждом пользователе
   - Назначение/снятие прав администратора
   - Сброс пароля пользователя
   - Удаление пользователя со всеми данными

3. **Безопасность**
   - Администратор не может удалить сам себя
   - Администратор не может снять права у самого себя
   - Все действия требуют подтверждения

## Как использовать

### Первый запуск

1. **Запустите сервер**:
   ```bash
   npm start
   ```

2. **Сделайте первого пользователя администратором**:
   
   Вариант 1 - через SQLite напрямую:
   ```bash
   sqlite3 database.db
   UPDATE users SET is_admin = 1 WHERE username = 'ваш_логин';
   .quit
   ```

   Вариант 2 - добавьте временный код в `server.js` после создания таблиц:
   ```javascript
   // Временный код: делаем первого пользователя админом
   db.run("UPDATE users SET is_admin = 1 WHERE id = 1", (err) => {
     if (err) console.error('Ошибка:', err);
     else console.log('✓ Первый пользователь стал админом');
   });
   ```

3. **Войдите на сайт** под этим пользователем

4. **Откройте админ-панель**:
   - Нажмите на кнопку "Войти" в шапке
   - В модальном окне появится кнопка "⚙️ Админ-панель" (только для администраторов)
   - Или перейдите напрямую на `/admin.html`

### Функции админ-панели

#### Просмотр пользователей
- Таблица показывает всех пользователей с:
  - ID, логин, email
  - Дата регистрации
  - Количество записей и логов воды
  - Роль (Админ/Пользователь)

#### Поиск
- Введите текст в поле поиска
- Ищет по логину, email и ID

#### Детали пользователя
- Нажмите кнопку "👁️ Детали"
- Откроется модальное окно с подробной информацией:
  - Дата последней записи
  - Дата последнего лога воды
  - Полная статистика

#### Управление правами
- Нажмите кнопку "🔐 Сделать админом" / "🔐 Снять админа"
- Подтвердите действие
- Пользователь сразу получит/потеряет права администратора

#### Сброс пароля
- Нажмите кнопку "🔑 Сбросить пароль"
- Введите новый пароль (минимум 4 символа)
- Пользователь сможет войти с новым паролем

#### Удаление пользователя
- Нажмите кнопку "🗑️ Удалить"
- Подтвердите действие
- **ВНИМАНИЕ**: Удаляются ВСЕ данные пользователя:
  - Записи прогресса
  - Настройки воды
  - Логи воды
  - Пользовательские настройки
  - Сам аккаунт

## Безопасность

### Middleware `requireAdmin`
Все админские endpoints защищены middleware, который:
1. Проверяет наличие JWT токена
2. Декодирует токен
3. Проверяет поле `is_admin` в БД
4. Возвращает 403 если пользователь не админ

### Клиентская проверка
- При открытии админ-панели сразу проверяются права
- Если пользователь не админ - редирект на главную страницу
- Кнопка "⚙️ Админ-панель" видна только администраторам

### Защита от ошибок
- Нельзя удалить себя
- Нельзя снять права у себя
- Все критичные действия требуют подтверждения

## Файлы

### Новые файлы:
- `admin.html` - страница админ-панели
- `admin-style.css` - стили админ-панели
- `admin.js` - логика админ-панели

### Изменённые файлы:
- `server.js` - добавлены API endpoints для администрирования
- `app.js` - добавлена проверка прав и кнопка админ-панели
- `index.html` - добавлена кнопка перехода в админ-панель

## Адаптивность

Админ-панель полностью адаптивна и работает на:
- Десктопах
- Планшетах
- Мобильных устройствах

На маленьких экранах:
- Таблица прокручивается горизонтально
- Кнопки действий выстраиваются вертикально
- Статистика в один столбец

## Стилизация

Админ-панель использует ту же цветовую схему, что и основное приложение:
- Темная тема
- Фиолетовый акцент (#7c3aed)
- Плавные переходы и hover-эффекты
- Современный дизайн с Space Grotesk шрифтом
