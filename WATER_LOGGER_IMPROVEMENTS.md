# 💧 Улучшения логгера воды

## Что добавлено

### 1. Выбор пола при регистрации 👤

При создании нового аккаунта пользователю теперь предлагается указать:
- **♂️ Парень** (мужской)
- **♀️ Девушка** (женский)

Выбранный пол автоматически сохраняется в БД и используется для расчета дневной нормы воды.

### 2. Умный расчет дневной нормы воды 🧮

Реализованы **разные формулы для мужчин и женщин**:

#### Для мужчин:
```
Дневная норма = (вес в кг × 35 мл) + 500 мл
Пример: парень весом 80 кг → (80 × 35) + 500 = 3300 мл
```

#### Для женщин:
```
Дневная норма = (вес в кг × 31 мл) + 300 мл
Пример: девушка весом 60 кг → (60 × 31) + 300 = 1860 мл
```

**Коррекция по уровню активности:**
- 🛋️ **Сидячий образ жизни** (sedentary): -10%
- 🚶 **Легкая активность** (light): ±0%
- 🏃 **Умеренная активность** (moderate): +10%
- 💪 **Активный** (active): +20%
- ⚡ **Очень активный** (very_active): +30%

### 3. Автоматический расчет при сохранении 🤖

Когда пользователь обновляет вес и уровень активности:
1. Система автоматически пересчитывает дневную норму
2. Новая норма применяется, если пользователь не указал свою собственную
3. Если пользователь указал свою норму - она сохраняется как есть

### 4. Базовые значения при первом входе

При первом входе (если нет сохраненных настроек):
- Вес: 70 кг (стандартное значение)
- Активность: Умеренная
- Дневная норма: рассчитывается автоматически на основе пола
- 4 быстрые кнопки с популярными напитками

## Технические детали

### БД
- Добавлено поле `gender` (TEXT) в таблицу `users` со значением по умолчанию 'male'
- Автоматическая миграция для существующих БД

### API Endpoints

#### Регистрация
```javascript
POST /api/signup
{
  username: "bohdan",
  email: "bohdan@example.com",
  password: "123456",
  gender: "male" // или "female"
}
```

#### Получение текущего пользователя
```javascript
GET /api/me
Response:
{
  id: 1,
  username: "bohdan",
  isAdmin: false,
  gender: "male"
}
```

#### Настройки воды
```javascript
GET /api/water-settings
Response:
{
  weight: 70,
  gender: "male",
  activity: "moderate",
  daily_goal: 2950, // автоматически рассчитано
  reset_time: "00:00",
  quick_buttons: [...]
}

POST /api/water-settings
{
  weight: 80,
  activity: "active",
  daily_goal: 0, // если 0 или не указано, пересчитывается
  reset_time: "00:00",
  quick_buttons: [...]
}
```

## Функция расчета

```javascript
function calculateDailyWaterGoal(weight, gender, activity) {
  // Базовый расчет
  let baseAmount = gender === 'female' 
    ? Math.round(weight * 31 + 300)
    : Math.round(weight * 35 + 500);
  
  // Коррекция по активности
  const multipliers = {
    sedentary: 0.9,
    light: 1.0,
    moderate: 1.1,
    active: 1.2,
    very_active: 1.3
  };
  
  return Math.round(baseAmount * multipliers[activity]);
}
```

## Примеры расчета

### Женщина 60 кг, умеренная активность:
- Base: (60 × 31) + 300 = 1860 мл
- С активностью ×1.1: 1860 × 1.1 = **2046 мл**

### Мужчина 85 кг, активный образ жизни:
- Base: (85 × 35) + 500 = 3475 мл
- С активностью ×1.2: 3475 × 1.2 = **4170 мл**

### Женщина 55 кг, сидячая работа:
- Base: (55 × 31) + 300 = 2005 мл
- С активностью ×0.9: 2005 × 0.9 = **1804 мл**

## Фронтенд

### Форма регистрации
- Добавлен выпадающий список (select) для выбора пола
- Отображается с эмодзи для наглядности (♂️ / ♀️)
- Значение передается на сервер при создании аккаунта

### Логика
- При загрузке настроек воды автоматически учитывается пол пользователя
- При сохранении новых параметров вес и активность пересчитываются
- Дневная норма обновляется в реальном времени

## Миграция существующих пользователей

При запуске сервера автоматически:
1. Проверяется наличие поля `gender` в БД
2. Если отсутствует - добавляется со значением 'male' для всех существующих пользователей
3. Это позволяет сохранить все старые данные без потерь

## Тестирование

### Тест 1: Новая регистрация
1. Откройте приложение
2. Создайте новый аккаунт
3. Выберите пол
4. Проверьте, что дневная норма рассчитана правильно

### Тест 2: Обновление параметров
1. Войдите в аккаунт
2. Откройте настройки воды
3. Измените вес и активность
4. Проверьте, что дневная норма автоматически пересчиталась

### Тест 3: Разные полы
1. Создайте двух пользователей (мужчину и женщину) с одинаковым весом
2. Проверьте, что нормы отличаются (женщина - меньше)
